export interface personData {
    username : string,
    first_name : string,
    last_name : string
}

export interface Section {
    id : number,
    name : string,
    data : string,
    level : number,
    document : number
}